name: 'czech_banking'
version: '1.0.0'

profile: 'czech_banking'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  czech_banking:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
      finance:
        fct_transactions:
          +materialized: incremental
